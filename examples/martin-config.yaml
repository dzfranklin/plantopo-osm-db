web_ui: enableforall

postgres:
  - connection_string: "postgresql://osm@host.docker.internal:5433/osm"
    auto_publish:
      functions:
        from_schemas: [ osm_functions ]

styles:
  - /examples
